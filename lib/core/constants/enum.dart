enum TypeProduct { drink, food }

enum LoadStatus {
  initial,
  loading,
  loadingMore,
  success,
  reload,
  failure;
}

enum TypeKeyBoard {
  normal,
  payment,
}

enum Screen { setting, restaurent, invoice }
